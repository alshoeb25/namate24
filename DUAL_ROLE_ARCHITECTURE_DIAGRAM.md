# 🏗️ Dual Role System - Visual Architecture

## System Overview Diagram

```
┌─────────────────────────────────────────────────────────────────────┐
│                         USER REGISTRATION                            │
├─────────────────────────────────────────────────────────────────────┤
│                                                                       │
│   /register?role=teacher          /register?role=student            │
│            │                                  │                      │
│            ▼                                  ▼                      │
│   ┌─────────────────┐              ┌─────────────────┐             │
│   │  Registration   │              │  Registration   │             │
│   │  Form (Teacher) │              │  Form (Student) │             │
│   └────────┬────────┘              └────────┬────────┘             │
│            │                                  │                      │
│            ▼                                  ▼                      │
│   POST /api/register                POST /api/register              │
│   { role: 'teacher' }               { role: 'student' }             │
│            │                                  │                      │
│            ▼                                  ▼                      │
│   ┌──────────────────┐            ┌──────────────────┐             │
│   │ users table      │            │ users table      │             │
│   │ + tutors table   │            │ + students table │             │
│   └────────┬─────────┘            └────────┬─────────┘             │
│            │                                  │                      │
│            ▼                                  ▼                      │
│   /tutor/profile                    /student/dashboard              │
│                                                                       │
└─────────────────────────────────────────────────────────────────────┘
```

---

## Database Relationships

```
┌──────────────────────────────────────────────────────────────┐
│                      USERS TABLE (Main)                       │
│  ┌────────────────────────────────────────────────────────┐  │
│  │ id (PK)  │  name  │  email  │  password  │  avatar    │  │
│  │    1     │  John  │  j@.com │  ********  │  pic.jpg   │  │
│  └────────────────────────────────────────────────────────┘  │
│                    │                    │                      │
│                    │                    │                      │
│      ┌─────────────┘                    └─────────────┐       │
│      │                                                 │       │
│      ▼                                                 ▼       │
│  ┌────────────────────┐               ┌────────────────────┐ │
│  │   TUTORS TABLE     │               │  STUDENTS TABLE    │ │
│  ├────────────────────┤               ├────────────────────┤ │
│  │ id (PK)            │               │ id (PK)            │ │
│  │ user_id (FK,unique)│               │ user_id (FK,unique)│ │
│  │ photo_url          │               │ grade_level        │ │
│  │ bio                │               │ learning_goals     │ │
│  │ subjects (JSON)    │               │ created_at         │ │
│  │ hourly_rate        │               │ updated_at         │ │
│  │ experience_years   │               └────────────────────┘ │
│  │ is_verified        │                                       │
│  │ created_at         │                                       │
│  │ updated_at         │                                       │
│  └────────────────────┘                                       │
│                                                                │
│  Relationship: One-to-One (optional) on both sides            │
│  - User may have 0 or 1 tutor record                          │
│  - User may have 0 or 1 student record                        │
│  - User can have BOTH tutor and student records               │
└──────────────────────────────────────────────────────────────┘
```

---

## User Authentication Flow

```
┌──────────────────────────────────────────────────────────────────┐
│                        LOGIN PROCESS                              │
└──────────────────────────────────────────────────────────────────┘

    User enters email + password
              │
              ▼
    POST /api/login
              │
              ▼
    ┌─────────────────────────┐
    │  Backend validates      │
    │  Returns token + user   │
    └─────────┬───────────────┘
              │
              ▼
    {
      token: "eyJ0eXAi...",
      user: {
        id: 1,
        name: "John Doe",
        email: "john@example.com",
        avatar: "profile.jpg",
        tutor: {               ← Eager loaded relationship
          id: 5,
          user_id: 1,
          photo_url: "...",
          bio: "...",
          ...
        },
        student: {             ← Eager loaded relationship
          id: 3,
          user_id: 1,
          grade_level: "...",
          ...
        }
      }
    }
              │
              ▼
    ┌──────────────────────────┐
    │ Frontend stores:         │
    │ - Token in localStorage  │
    │ - User in Pinia store    │
    └─────────┬────────────────┘
              │
              ▼
    ┌─────────────────────────────────────────┐
    │  Check user roles and redirect:         │
    │                                          │
    │  if (user.tutor && user.student)        │
    │    → /tutor/dashboard OR /student/...   │
    │  else if (user.tutor)                   │
    │    → /tutor/profile                     │
    │  else if (user.student)                 │
    │    → /student/dashboard                 │
    │  else                                   │
    │    → / (home with enrollment options)   │
    └─────────────────────────────────────────┘
```

---

## Enrollment Flow Diagram

```
┌──────────────────────────────────────────────────────────────────────┐
│                    EXISTING USER ENROLLS IN NEW ROLE                  │
└──────────────────────────────────────────────────────────────────────┘

User logged in (has tutor record, no student record)
          │
          ▼
┌──────────────────────────┐
│  Clicks avatar           │
│  User menu opens         │
└─────────┬────────────────┘
          │
          ▼
┌────────────────────────────────────────────┐
│  User Menu Display:                        │
│                                             │
│  🎓 TEACHER / EXPERT                       │
│  ✓ Tutor Dashboard  (already enrolled)     │
│                                             │
│  📚 STUDENT / PARENT                       │
│  ⊕ Enroll as Student  (button)             │
└─────────┬──────────────────────────────────┘
          │
          ▼
User clicks "Enroll as Student"
          │
          ▼
┌──────────────────────────────────────────┐
│  EnrollmentModal.vue opens               │
│                                           │
│  ┌─────────────────────────────────────┐ │
│  │  Enroll as Student                  │ │
│  │  ─────────────────────────────────  │ │
│  │                                     │ │
│  │  Create your student profile and   │ │
│  │  start finding perfect tutors.     │ │
│  │                                     │ │
│  │  What you'll get:                  │ │
│  │  ✓ Access to student dashboard     │ │
│  │  ✓ Search and find tutors          │ │
│  │  ✓ Post tutor requirements         │ │
│  │  ✓ Connect with teachers           │ │
│  │                                     │ │
│  │  [Enroll as Student] button        │ │
│  └─────────────────────────────────────┘ │
└─────────┬────────────────────────────────┘
          │
          ▼
User clicks "Enroll as Student" button
          │
          ▼
POST /api/user/enroll-student
          │
          ▼
┌────────────────────────────────────────┐
│  Backend:                              │
│  1. Checks if student record exists    │
│  2. Creates student record:            │
│     INSERT INTO students               │
│     (user_id, created_at, updated_at)  │
│     VALUES (1, NOW(), NOW())           │
│  3. Returns student object             │
└─────────┬──────────────────────────────┘
          │
          ▼
Frontend: userStore.fetchUser()
          │
          ▼
GET /api/user (with relationships)
          │
          ▼
User object updated in store:
{
  id: 1,
  name: "John Doe",
  tutor: { ... },    ← Still has tutor record
  student: { ... }   ← Now has student record too!
}
          │
          ▼
┌──────────────────────────────────────┐
│  Success animation in modal          │
│  "Enrollment Successful!"            │
│  Redirecting...                      │
└─────────┬────────────────────────────┘
          │
          │ (after 2 seconds)
          ▼
router.push('/student/dashboard')
          │
          ▼
┌────────────────────────────────────────┐
│  Student Dashboard loads               │
│  User now has access to BOTH:          │
│  - /tutor/* routes (via tutor record)  │
│  - /student/* routes (via student rec) │
└────────────────────────────────────────┘
```

---

## Route Protection Logic

```
┌─────────────────────────────────────────────────────────────────┐
│                   NAVIGATION GUARD FLOW                          │
└─────────────────────────────────────────────────────────────────┘

User navigates to /tutor/profile
          │
          ▼
router.beforeEach((to, from, next) => {
          │
          ▼
    ┌──────────────────────────────┐
    │ Check: Is token present?     │
    └─────┬────────────────────────┘
          │
    ┌─────┴─────┐
    │ No        │ Yes
    │           │
    ▼           ▼
  next('/login') ┌──────────────────────────────┐
                 │ Fetch user from store        │
                 └─────┬────────────────────────┘
                       │
                       ▼
                 ┌──────────────────────────────┐
                 │ Check: Route starts with     │
                 │ /tutor/ ?                    │
                 └─────┬────────────────────────┘
                       │
                 ┌─────┴─────┐
                 │ No        │ Yes
                 │           │
                 ▼           ▼
           (skip check) ┌────────────────────┐
                        │ Does user.tutor    │
                        │ exist?             │
                        └─────┬──────────────┘
                              │
                        ┌─────┴─────┐
                        │ No        │ Yes
                        │           │
                        ▼           ▼
                   next('/')   next() ✓
                   (redirect)  (allow access)


Same logic for /student/* routes checking user.student
Same logic for /profile checking authentication only
})
```

---

## Component Hierarchy

```
┌────────────────────────────────────────────────────────────────┐
│                         APP.VUE                                 │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │                    <router-view>                         │  │
│  └────────┬─────────────────────────────────┬───────────────┘  │
│           │                                  │                  │
│           ▼                                  ▼                  │
│  ┌──────────────────┐            ┌──────────────────┐          │
│  │  Authenticated   │            │  Public Routes   │          │
│  │  Routes          │            │  (Login, Reg)    │          │
│  └────────┬─────────┘            └──────────────────┘          │
│           │                                                     │
│           ▼                                                     │
│  ┌──────────────────────────────────────────────┐              │
│  │          AuthLayout.vue                      │              │
│  │  ┌────────────────────────────────────────┐  │              │
│  │  │  AuthTopBar.vue                        │  │              │
│  │  │  ┌──────────────────────────────────┐  │  │              │
│  │  │  │  Profile dropdown menu           │  │  │              │
│  │  │  │  ┌────────────────────────────┐  │  │  │              │
│  │  │  │  │ Teacher Section:           │  │  │  │              │
│  │  │  │  │ - Tutor Dashboard (link)   │  │  │  │              │
│  │  │  │  │ - Enroll button            │  │  │  │              │
│  │  │  │  └────────────────────────────┘  │  │  │              │
│  │  │  │  ┌────────────────────────────┐  │  │  │              │
│  │  │  │  │ Student Section:           │  │  │  │              │
│  │  │  │  │ - Student Dashboard (link) │  │  │  │              │
│  │  │  │  │ - Enroll button            │  │  │  │              │
│  │  │  │  └────────────────────────────┘  │  │  │              │
│  │  │  └──────────────────────────────────┘  │  │              │
│  │  └────────────────────────────────────────┘  │              │
│  │  ┌────────────────────────────────────────┐  │              │
│  │  │  Secondary Menu (context-based)        │  │              │
│  │  │  - TutorSecondaryMenu.vue              │  │              │
│  │  │  - StudentSecondaryMenu.vue            │  │              │
│  │  └────────────────────────────────────────┘  │              │
│  │  ┌────────────────────────────────────────┐  │              │
│  │  │  <router-view> (nested routes)         │  │              │
│  │  │  - Tutor pages OR Student pages        │  │              │
│  │  └────────────────────────────────────────┘  │              │
│  └──────────────────────────────────────────────┘              │
│                                                                 │
│  ┌──────────────────────────────────────────────┐              │
│  │  EnrollmentModal.vue                         │              │
│  │  (Teleported/Portal - fixed position)        │              │
│  │  - Triggered by enroll buttons               │              │
│  │  - Handles enrollment API call               │              │
│  │  - Shows loading/success/error states        │              │
│  └──────────────────────────────────────────────┘              │
└────────────────────────────────────────────────────────────────┘
```

---

## State Management (Pinia Store)

```
┌────────────────────────────────────────────────────────────────┐
│                      PINIA STORE STRUCTURE                      │
│                        (store/index.js)                         │
└────────────────────────────────────────────────────────────────┘

export const useUserStore = defineStore('user', {
  
  ┌────────────────────────────────────────┐
  │  STATE                                 │
  └────────────────────────────────────────┘
    user: null,        // User object with relationships
    token: null,       // JWT token
    isLoading: false   // Loading state

  ┌────────────────────────────────────────┐
  │  GETTERS                               │
  └────────────────────────────────────────┘
    isAuthenticated: (state) => !!state.token
    isTeacher: (state) => !!state.user?.tutor
    isStudent: (state) => !!state.user?.student
    isDualRole: (state) => !!state.user?.tutor && !!state.user?.student

  ┌────────────────────────────────────────┐
  │  ACTIONS                               │
  └────────────────────────────────────────┘
    ┌────────────────────────────────┐
    │  login(email, password)        │
    │  ├─ POST /api/login            │
    │  ├─ Store token                │
    │  └─ Store user with relations  │
    └────────────────────────────────┘

    ┌────────────────────────────────┐
    │  register(data)                │
    │  ├─ POST /api/register         │
    │  ├─ Store token                │
    │  └─ Store user with relations  │
    └────────────────────────────────┘

    ┌────────────────────────────────┐
    │  fetchUser()                   │
    │  ├─ GET /api/user              │
    │  └─ Update user with relations │
    └────────────────────────────────┘

    ┌──────────────────────────────────┐
    │  enrollAsTeacher()               │
    │  ├─ POST /api/user/enroll-teacher│
    │  ├─ fetchUser() (refresh)        │
    │  └─ Return response              │
    └──────────────────────────────────┘

    ┌──────────────────────────────────┐
    │  enrollAsStudent()               │
    │  ├─ POST /api/user/enroll-student│
    │  ├─ fetchUser() (refresh)        │
    │  └─ Return response              │
    └──────────────────────────────────┘

    ┌────────────────────────────────┐
    │  logout()                      │
    │  ├─ POST /api/logout           │
    │  ├─ Clear token                │
    │  └─ Clear user                 │
    └────────────────────────────────┘
})
```

---

## API Request/Response Flow

```
┌──────────────────────────────────────────────────────────────────┐
│               ENROLLMENT API FLOW (Example)                       │
└──────────────────────────────────────────────────────────────────┘

Frontend                     Backend                      Database
    │                           │                             │
    │  POST /api/user/         │                             │
    │  enroll-teacher          │                             │
    │  ─────────────────────>  │                             │
    │  Headers:                │                             │
    │  Authorization:          │                             │
    │  Bearer <token>          │                             │
    │                          │                             │
    │                          │  1. Validate token          │
    │                          │  2. Get authenticated       │
    │                          │     user ID                 │
    │                          │                             │
    │                          │  3. Check if tutor exists   │
    │                          │  ──────────────────────────>│
    │                          │  SELECT * FROM tutors       │
    │                          │  WHERE user_id = ?          │
    │                          │  <───────────────────────── │
    │                          │  (returns empty)            │
    │                          │                             │
    │                          │  4. Create tutor record     │
    │                          │  ──────────────────────────>│
    │                          │  INSERT INTO tutors         │
    │                          │  (user_id, created_at,      │
    │                          │   updated_at) VALUES        │
    │                          │  (1, NOW(), NOW())          │
    │                          │  <───────────────────────── │
    │                          │  (success, new ID: 5)       │
    │                          │                             │
    │  <─────────────────────  │                             │
    │  Response:               │                             │
    │  {                       │                             │
    │    message: "Success",   │                             │
    │    tutor: {              │                             │
    │      id: 5,              │                             │
    │      user_id: 1,         │                             │
    │      ...                 │                             │
    │    }                     │                             │
    │  }                       │                             │
    │                          │                             │
    │  5. Call fetchUser()     │                             │
    │  ─────────────────────>  │                             │
    │  GET /api/user           │                             │
    │                          │                             │
    │                          │  6. Load user with relations│
    │                          │  ──────────────────────────>│
    │                          │  SELECT users.*,            │
    │                          │  tutors.*, students.*       │
    │                          │  FROM users                 │
    │                          │  LEFT JOIN tutors           │
    │                          │  LEFT JOIN students         │
    │                          │  WHERE users.id = 1         │
    │                          │  <───────────────────────── │
    │                          │                             │
    │  <─────────────────────  │                             │
    │  {                       │                             │
    │    id: 1,                │                             │
    │    name: "John",         │                             │
    │    tutor: { id: 5, ...}, │  ← Now populated!          │
    │    student: null         │                             │
    │  }                       │                             │
    │                          │                             │
    │  7. Update Pinia store   │                             │
    │  8. Redirect to          │                             │
    │     /tutor/profile       │                             │
    └──────────────────────────┴─────────────────────────────┘
```

---

## User Journey Map

```
┌────────────────────────────────────────────────────────────────────┐
│                    USER JOURNEY: Dual Role                          │
└────────────────────────────────────────────────────────────────────┘

Day 1: Register as Student
    │
    ├─ Visit site as new user
    ├─ Click "Register"
    ├─ Select "Student/Parent" role
    ├─ Fill form and submit
    ├─ Account created with student record
    ├─ Redirected to /student/dashboard
    ├─ Explore student features:
    │  ├─ Find Tutor (search)
    │  ├─ Request Tutor (12-step form)
    │  └─ View Requirements
    └─ Logout

Day 2: Login & Discover Teacher Option
    │
    ├─ Login with same email
    ├─ Directed to /student/dashboard
    ├─ Click avatar → user menu
    ├─ Notice "Enroll as Teacher" button
    ├─ Think: "I could teach too!"
    └─ Continue as student for now

Day 3: Enroll as Teacher
    │
    ├─ Login
    ├─ Click avatar → "Enroll as Teacher"
    ├─ Modal appears with benefits
    ├─ Click "Enroll as Teacher" button
    ├─ Success! Now dual role
    ├─ Redirected to /tutor/profile
    ├─ Fill out teaching profile:
    │  ├─ Upload photo
    │  ├─ Write bio
    │  ├─ Add subjects
    │  ├─ Set hourly rate
    │  └─ Save
    └─ Profile complete

Day 4+: Using Both Roles
    │
    ├─ Login (goes to last visited dashboard)
    ├─ User menu shows BOTH options:
    │  ├─ Tutor Dashboard
    │  └─ Student Dashboard
    │
    ├─ Morning: Acting as Teacher
    │  ├─ Click "Tutor Dashboard"
    │  ├─ View student requirements
    │  ├─ Respond to inquiries
    │  └─ Manage teaching schedule
    │
    └─ Afternoon: Acting as Student
       ├─ Click "Student Dashboard"
       ├─ Search for advanced tutors
       ├─ Post new requirement
       └─ Check wallet balance

Result: Seamless dual-role experience with single account!
```

---

## Color Scheme & Branding

```
┌────────────────────────────────────────────────────────────────┐
│                     VISUAL DESIGN SYSTEM                        │
└────────────────────────────────────────────────────────────────┘

TEACHER/TUTOR THEME
  Primary: Pink (#EC4899)
  Secondary: Purple (#9333EA)
  Gradient: from-pink-500 to-purple-600
  Hover: bg-pink-50, text-pink-600
  Icons: fas fa-chalkboard-teacher, fa-user-tie

  Used in:
  - Tutor dashboard cards
  - Teacher section in user menu
  - Enrollment modal (teacher)
  - Tutor secondary menu hover
  - Profile badges

STUDENT THEME
  Primary: Blue (#3B82F6)
  Secondary: Cyan (#06B6D4)
  Gradient: from-blue-500 to-cyan-600
  Hover: bg-blue-50, text-blue-600
  Icons: fas fa-user-graduate, fa-graduation-cap

  Used in:
  - Student dashboard cards
  - Student section in user menu
  - Enrollment modal (student)
  - Student secondary menu hover
  - Request form progress bar

NEUTRAL/SHARED
  Background: White (#FFFFFF)
  Text: Gray-700 (#374151)
  Border: Gray-200 (#E5E7EB)
  Success: Green (#10B981)
  Error: Red (#EF4444)
  Warning: Yellow (#F59E0B)

  Used in:
  - AuthTopBar
  - Profile management
  - Modal backgrounds
  - Form inputs
  - General UI elements
```

---

## File Structure Summary

```
resources/js/
├── components/
│   ├── EnrollmentModal.vue                 ← New: Enrollment UI
│   ├── header/
│   │   ├── AuthTopBar.vue                  ← Modified: Added enrollment
│   │   ├── StudentSecondaryMenu.vue        ← New: Student nav
│   │   └── TutorSecondaryMenu.vue          ← Existing: Tutor nav
│   └── layout/
│       ├── StudentLayout.vue               ← New: Student wrapper
│       └── TutorLayout.vue                 ← Existing: Tutor wrapper
├── pages/
│   ├── auth/
│   │   ├── Login.vue
│   │   ├── Register.vue                    ← Modified: Role selection
│   │   └── GoogleCallback.vue
│   ├── profile/
│   │   └── ProfileManagement.vue           ← New: Universal profile
│   ├── student/
│   │   ├── StudentDashboard.vue            ← New
│   │   ├── RequestTutor.vue                ← New: 12-step form
│   │   ├── RequirementsList.vue            ← New
│   │   ├── StudentWallet.vue               ← New
│   │   ├── StudentReviews.vue              ← New
│   │   └── StudentSettings.vue             ← New
│   └── tutor/
│       ├── TutorDashboard.vue
│       └── TutorProfile.vue
├── router/
│   └── index.js                            ← Modified: Added guards
└── store/
    └── index.js                            ← Modified: Added enrollment

Documentation:
├── DUAL_ROLE_API_REQUIREMENTS.md          ← API specification
├── DUAL_ROLE_IMPLEMENTATION.md            ← Implementation guide
├── DUAL_ROLE_QUICK_START.md               ← Quick start guide
├── DUAL_ROLE_TESTING_CHECKLIST.md         ← Testing checklist
└── DUAL_ROLE_ARCHITECTURE_DIAGRAM.md      ← This file!
```

---

## Next Steps for Backend Developer

1. **Review Documentation**
   - Read `DUAL_ROLE_API_REQUIREMENTS.md` for endpoint specs
   - Check `DUAL_ROLE_QUICK_START.md` for overview
   - Use `DUAL_ROLE_TESTING_CHECKLIST.md` for validation

2. **Create Migrations**
   ```bash
   php artisan make:migration create_tutors_table
   php artisan make:migration create_students_table
   php artisan migrate
   ```

3. **Update Models**
   - Add relationships to User model
   - Create Tutor and Student models
   - Test relationships in Tinker

4. **Implement API Endpoints**
   - Start with enrollment endpoints
   - Test with Postman
   - Ensure eager loading of relationships

5. **Test Integration**
   - Use frontend to test enrollment
   - Verify database records created
   - Check user object structure in API responses

6. **Deploy**
   - Run migrations on production
   - Update .env if needed
   - Monitor error logs

---

**Frontend Status**: ✅ Complete and ready
**Backend Status**: ⏳ Awaiting implementation
**Integration Status**: 🔜 Ready to test after backend complete
